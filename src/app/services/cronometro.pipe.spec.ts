import { CronometroPipe } from './cronometro.pipe';

describe('CronometroPipe', () => {
  it('create an instance', () => {
    const pipe = new CronometroPipe();
    expect(pipe).toBeTruthy();
  });
});
